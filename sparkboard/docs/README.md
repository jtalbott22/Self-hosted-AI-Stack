# Screenshots

Put dashboard screenshots here and reference them from the top-level README:

- `dashboard.png` — the full dashboard (memory pool, readout rail, charts)
- `containers.png` — the containers and listening-ports tables
- `activity-feed.png` — the LLM activity feed (if you run it)

PNG at roughly 1400px wide looks good on GitHub. Dark theme screenshots read
best against GitHub's default, but include a light-theme shot too if you like.
